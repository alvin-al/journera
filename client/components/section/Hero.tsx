import React from "react";
import Navigation from "../element/Navigation";
import HeroBanner from "../element/HeroBanner";

const Hero = () => {
  return (
    <div>
      <HeroBanner />
    </div>
  );
};

export default Hero;
